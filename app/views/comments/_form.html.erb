<%= form_for([@micropost,@micropost.comments.build ]) do |f| %>
<div class="comments-add">
<h4> Create new comment</h4>
 <h4 class="comments-count"><%= @micropost.comments.count%>Comments</h4>
  <div class="form-input">
  <%= f.text_area :body%>
  </div>
  <button class="form-btn">
    <%= f.submit%>
  </button></div>

<% end %>
